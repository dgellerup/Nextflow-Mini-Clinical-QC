# Nextflow-Mini-Clinical-QC
Analyzes and creates an HTML quality control and mapping report for clinical FASTQ files

## Execution
`nextflow run main.nf --input { Path/To/Your/Data } --outdir { Path/To/Results/Dir }`